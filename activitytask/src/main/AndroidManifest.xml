<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sber.android.activitytask">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_School">
        <activity
            android:name="ru.sber.android.activitytask.ActivitySingleInstance"
            android:launchMode="singleInstance" />
        <activity
            android:name="ru.sber.android.activitytask.ActivitySingleTop"
            android:launchMode="singleTop" />
        <activity
            android:name="ru.sber.android.activitytask.ActivityStandard"
            android:launchMode="standard" />
        <activity
            android:name="ru.sber.android.activitytask.ActivitySingleTask"
            android:launchMode="singleTop" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>